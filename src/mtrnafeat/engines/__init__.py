"""External RNA-folding engine wrappers.

Each engine module exposes a small functional API that returns plain
Python types so the rest of the package never imports an engine binding
or shells out directly.
"""
